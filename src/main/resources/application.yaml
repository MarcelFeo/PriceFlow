spring:
  application:
    name: priceflow

  datasource:
    url: jdbc:postgresql://ep-lively-snow-an5kl8vn-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_G4JFzKeE0Rsw
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    baseline-on-migrate: true # Útil se o seu banco já tiver tabelas criadas manualmente
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect